# OpenHuntX WebGuard CWE Coverage

## Status language

This registry follows the same discipline as [`ROADMAP.md`](ROADMAP.md): it separates what is implemented and tested today from what is planned. It is not a claim that WebGuard detects every weakness listed, and it is not a transcription of the full CWE catalog.

## Scope discipline

WebGuard does not claim to scan for every CWE. The CWE catalog (including the reference view consulted while scoping this registry) covers roughly a thousand entries spanning memory safety, language-specific misuse, hardware, and internal software-development defects that are not observable by an external, black-box web assessment tool. Those entries are out of scope by construction and are not enumerated here as "not applicable" noise.

This registry instead tracks only CWE classes that are:

- web- or API-relevant, and
- externally observable (or detectable with explicit, authorised authenticated access) without source-code review.

Each tracked CWE has one status:

- **IMPLEMENTED**: detection logic exists in a scanner analyzer, runs as part of a normal scan, and is covered by tests.
- **IMPLEMENTED (active, permit-gated)**: detector code exists, is tested, and is reachable end-to-end through the API job/executor pipeline when a TrustScan permit explicitly authorizes it (`active_checks` claim). Not reachable through the standalone `webguard scan` CLI, which does not use TrustScan permits at all. See the linked audit doc for exactly what was and wasn't validated.
- **PARTIAL**: some indicators can be detected, but exploitability cannot always be established from the outside.
- **PLANNED**: defined as in-scope, not yet implemented.
- **NOT APPLICABLE**: cannot reasonably be tested externally, intentionally excluded.

## Implemented

Detected today by the passive analyzers (`workers/scanner/src/webguard_scanner/`), each with dedicated tests:

| CWE | Name | Detected by |
|---|---|---|
| CWE-16 | Configuration | `cors_analyzer.py`, `header_analyzer.py`, `cookie_analyzer.py` |
| CWE-20 | Improper Input Validation | `cookie_analyzer.py` |
| CWE-200 | Exposure of Sensitive Information to an Unauthorized Actor | `header_analyzer.py`, `html_analyzer.py`, `disclosure_analyzer.py` |
| CWE-209 | Generation of Error Message Containing Sensitive Information | `html_analyzer.py` |
| CWE-295 | Improper Certificate Validation | `tls_analyzer.py` |
| CWE-319 | Cleartext Transmission of Sensitive Information | `header_analyzer.py`, `html_analyzer.py`, `cookie_analyzer.py` |
| CWE-324 | Use of a Key Past its Expiration Date | `tls_analyzer.py` |
| CWE-326 | Inadequate Encryption Strength | `tls_analyzer.py` |
| CWE-327 | Use of a Broken or Risky Cryptographic Algorithm | `tls_analyzer.py` |
| CWE-548 | Exposure of Information Through Directory Listing | `html_analyzer.py` |
| CWE-598 | Use of GET Request Method With Sensitive Query Strings | `html_analyzer.py` |
| CWE-614 | Sensitive Cookie in HTTPS Session Without 'Secure' Attribute | `cookie_analyzer.py` |
| CWE-615 | Sensitive Information in Source Comments | `html_analyzer.py` |
| CWE-693 | Protection Mechanism Failure | `header_analyzer.py` |
| CWE-942 | Overly Permissive Cross-domain Whitelist | `cors_analyzer.py` |
| CWE-1004 | Sensitive Cookie Without 'HttpOnly' Flag | `cookie_analyzer.py` |
| CWE-1021 | Improper Restriction of Rendered UI Layers or Frames | `header_analyzer.py` |
| CWE-1275 | Sensitive Cookie with Improper SameSite Attribute | `cookie_analyzer.py` |

**18 CWEs implemented**, all passive (no active/intrusive requests), all covered by unit tests.

## Implemented (active, permit-gated, operator-reachable)

| CWE | Name | Detector | Notes |
|---|---|---|---|
| CWE-79 | Improper Neutralization of Input During Web Page Generation ('Cross-site Scripting') | `xss_reflected_detector.py` | **Operator-reachable + authorization-controlled + end-to-end verified.** Reflected-XSS over GET query/form parameters and, as of Slice 6, POST-form parameters (via the shared request-template/mutation engine, gated by the permit's `allowed_http_methods` claim). JSON bodies are deliberately not supported: this detector's evidence (unescaped markup echoed into an HTML response) doesn't mean the same thing for a JSON API response. Issuable via `webguard-api permit issue --active-check active.xss.reflected`, gated by a stricter owner-only RBAC permission (`PERMIT_ISSUE_ACTIVE`) beyond ordinary permit issuance, and audited (detector ID recorded, never payload/evidence). Runs end-to-end through the job executor when the bound TrustScan permit's `active_checks` claim authorizes it; every existing and default-issued permit has an empty `active_checks` claim and therefore never triggers it, fail-closed by default. A true end-to-end test (CLI → HTTP API → worker → executor → registry → detector → report) is verified against a local synthetic fixture, over real TLS, real permit/RBAC enforcement, and the real runtime safety engine. Not reachable through the standalone `webguard scan` CLI (no permit concept there). See `docs/audit/active-detection-phase1-xss.md` (detector validation), `docs/audit/active-detection-phase2-orchestration.md` (orchestration wiring), `docs/audit/active-detection-phase3-operator-surface.md` (CLI/RBAC/audit surface + the end-to-end test), and `docs/audit/active-detection-phase6-request-mutation.md` (POST-form support) for exactly what was validated at each layer. |
| CWE-89 | Improper Neutralization of Special Elements used in an SQL Command ('SQL Injection') | `sqli_error_detector.py` | **Operator-reachable + authorization-controlled + end-to-end verified.** Conservative, error-based detection only (baseline request + one bounded apostrophe diagnostic per candidate; no boolean-differential, time-based, UNION, or stacked-query techniques), unchanged since Slice 4. As of Slice 6, this same methodology runs over three input transports through the shared request-template/mutation engine: GET query parameter, POST form parameter, and JSON body parameter (dotted/indexed path, e.g. `user.email`), each independently gated by the permit's `allowed_http_methods` claim in addition to `active_checks`. A finding requires a specific, database-engine-attributable error signature present in the diagnostic response and absent from the baseline, regardless of transport. A generic status-code change alone never produces a finding. Issuable via `webguard-api permit issue --active-check active.sqli.error`, independently authorized from `active.xss.reflected` (proven for both GET and POST transports: an XSS-only permit cannot trigger SQLi and vice versa, even against a field genuinely vulnerable to both). Verified over a real, purpose-built, deliberately vulnerable local SQLite-backed fixture (real sockets, real database engine) with three transports × vulnerable/safe pairs plus a generic-500 and a database-looking-static-text negative control, none of which produced a false finding. A true end-to-end test (CLI → HTTP API → worker → executor → registry → detector → report) is verified the same way as CWE-79. Investigated against the pinned Juice Shop lab target: **still could not be validated there, for a documented reason that changed with this slice.** Juice Shop's real login endpoint (`POST /rest/user/login`, JSON body) is now representable and, if manually supplied, was empirically confirmed to be correctly classified INCONCLUSIVE by this detector's unmodified error-based methodology (no error signature: Juice Shop's login bypass is a silent boolean/auth-bypass condition, not an error condition). It remains undiscovered by static discovery (no HTML form, no real OpenAPI/sitemap/robots reference: Juice Shop is an Angular SPA whose API surface loads via JS bundles this project deliberately does not parse). See `docs/audit/active-detection-phase4-sqli.md` (GET, phase 4) and `docs/audit/active-detection-phase6-request-mutation.md` (POST/JSON, phase 6) for exactly what was and wasn't validated. |
| CWE-639 | Authorization Bypass Through User-Controlled Key (IDOR/BOLA) | `idor_authorization_detector.py` | **Operator-reachable + authorization-controlled + end-to-end verified; and, uniquely among this project's active detectors so far, confirmed against a live, real-world lab target, not only a synthetic fixture.** A genuine multi-identity authorization-comparison engine, not a parameter fuzzer: requires two explicitly registered, distinct `AuthenticationContext`s (`AuthorizationComparisonPlan`, a separate signed permit claim from the single-identity `authentication_context_id`) and an explicit, operator-supplied `resource_scope` of controlled resource pairs: this detector never guesses, enumerates, or brute-forces an identifier; every resource identifier must originate from a resource created by a test identity, an explicitly configured fixture/test resource, or a controlled lab API response legitimately obtained while authenticated as that identity. Read-only (GET) object access only this slice. CONFIRMED requires an exact SHA-256 content-fingerprint match between the cross-identity response and the victim's own baseline response. A generic HTTP 200, or a coincidental response-length match, is never sufficient (a length-based fallback was tried, found to produce a real false positive in testing, and removed). A `SHARED`/`PUBLIC` expected-access classification on a resource makes it permanently unreportable regardless of what status both identities receive, so intentionally shared resources are never misreported as IDOR. Issuable via a permit whose `active_checks` includes `active.authorization.idor` **and** whose `authorization_comparison_plan_id` claim references a plan bound to this exact organization/target/authorization: referencing the plan without also requesting the check (or vice versa) is rejected; an XSS-only or SQLi-only permit cannot trigger it. Every finding carries both `CWE-639` and, as this project's first non-CWE identifier namespace, `OWASP-API` (`API1:2023`, Broken Object Level Authorization). `CWE-862` is deliberately never attached automatically, since this detector's evidence establishes a specific object-level bypass, not a general missing-authorization condition. Verified over a real, purpose-built fixture with two identities, a secure ownership-checked endpoint, a deliberately vulnerable parallel endpoint with no ownership check, and a shared-resource endpoint. The secure and shared endpoints never produce a finding. **Independently confirmed against a live OWASP Juice Shop container** using two accounts created only through Juice Shop's own documented registration/login endpoints and each account's own shopping-basket ID (obtained from that account's own JWT, never guessed). Juice Shop's real, publicly documented "view another user's basket" broken access control weakness was genuinely reproduced and correctly classified CONFIRMED/CWE-639 by this detector's unmodified methodology. A true end-to-end test (two contexts → comparison plan → permit → job → worker → executor → detector → report) is verified against the local synthetic fixture, over real TLS, real permit/RBAC enforcement, and the real runtime safety engine. See `docs/audit/active-detection-phase8-idor-bola.md` for exactly what was and wasn't validated. **Slice 9 adds authenticated resource *discovery* (an `AuthorizationComparisonPlan.enable_discovery` flag drives an authenticated crawl per identity, recognizing HTML-link and JSON-field identifiers with explicit `IdentifierProvenance` tagging and deterministic cross-identity comparison eligibility) as an alternative/additional source of resource pairs feeding this exact same, unmodified detector: no new CWE, no change to CONFIRMED/PROBABLE classification, shared/public exclusion, fingerprinting, or budgets. Re-confirmed against a live Juice Shop container using discovery instead of direct JWT inspection: the account's own basket ID was recognized from a JSON field its own login response legitimately contained (not guessed), still classified CONFIRMED/CWE-639. See `docs/audit/active-detection-phase9-authenticated-resource-discovery.md`. |
| CWE-918 | Server-Side Request Forgery (SSRF) | `ssrf_callback_detector.py` | **Operator-reachable + authorization-controlled + end-to-end verified, via a dedicated controlled-callback architecture rather than response-text guessing.** Confirmation requires a genuine, out-of-band server-side outbound request from the target to a WebGuard-controlled callback destination (`CallbackBroker`/`CallbackHttpReceiver`): a target response merely reflecting, validating, or mentioning a callback URL is never sufficient (verified directly with a false-positive fixture endpoint that echoes the URL and produces no finding). Candidate discovery filters the existing Slice 6 request-template model by URL-shaped parameter names (`url`, `webhook`, `image`, etc.) as a discovery-time heuristic only, never evidence by itself. Tokens are `secrets.token_urlsafe`-generated (never accepted from a caller), scan-bound, candidate-bound, time-limited, and bounded-use; an arbitrary or forged token, an expired token, or a callback from a different scan's token is never accepted as proof. CONFIRMED requires the callback to arrive within a bounded primary wait window; a later arrival within a secondary grace window is PROBABLE, never CONFIRMED. Never tests internal addresses (127.0.0.1, RFC1918, cloud metadata) to "prove" SSRF: the only destination ever used is a callback URL WebGuard itself issued, keeping this detector's own safety boundary (never probing internal networks) architecturally separate from WebGuard's pre-existing, unmodified outbound-request scope protections (`scope_validator.py`/`safe_http.py`, untouched by this slice). Verified over a real, purpose-built fixture (vulnerable/safe/reflect-only/validation-error/generic-500) with a real outbound fetch and a real local callback receiver, over real sockets, and via a true end-to-end permit → job → worker → executor → report pipeline. Every finding carries both `CWE-918` and `OWASP`/`A10:2021` (Server-Side Request Forgery is literally OWASP Top 10 2021's A10 category). Investigated against Juice Shop's real profile-image-URL-fetch feature: blocked by Juice Shop's own internal SSRF-challenge protection when using this slice's local-only callback destination, honestly recorded as no compatible controlled surface confirmed, rather than worked around by testing internal-network reachability (explicitly forbidden) or provisioning production callback infrastructure (explicitly out of scope this slice). See `docs/audit/active-detection-phase10-ssrf-callback.md` for exactly what was and wasn't validated. |
| CWE-22 | Improper Limitation of a Pathname to a Restricted Directory ('Path Traversal') | `path_traversal_detector.py` | **Registered, permit-gated, unit-tested. Not yet exercised end-to-end through the CLI/API/worker/executor pipeline, and not yet run against a live target.** Same two-request baseline/diagnostic methodology as `active.sqli.error`: one diagnostic payload (`../../../../../../etc/passwd`, six levels deep) per candidate, classified by whether a specific `/etc/passwd` content signature newly appears in the diagnostic response (absent from baseline). v1 tests only the Unix/Linux traversal target and this one payload; Windows-target traversal, alternate depths, and encoding-bypass variants are explicitly not attempted (stated directly in the module's own docstring, not silently assumed). Issuable via `webguard-api permit issue --active-check active.pathtraversal.disclosure`. Evidence sanitization (the matched signature text is never retained in the finding) and fingerprint determinism are both PROVEN by dedicated unit tests (`test_path_traversal_detector.py`, `test_finding_fingerprint_determinism.py::PathTraversalFingerprintDeterminismTests`). See `docs/audit/active-detection-phase11-path-traversal.md`. |
| CWE-78 | Improper Neutralization of Special Elements used in an OS Command ('OS Command Injection') | `command_injection_detector.py` | **Registered, permit-gated, unit-tested. Not yet exercised end-to-end through the CLI/API/worker/executor pipeline, and not yet run against a live target.** Diagnostic payload appends a POSIX shell separator and a comment terminator to the baseline value (`<baseline>; echo <fresh-random-marker> #`); CONFIRMED requires the marker to appear in the response *and* the full raw payload string to be absent, specifically to rule out a target that merely reflects an invalid parameter value verbatim into an error message (that false positive was found during this slice's own test-writing and fixed before merge, not caught later). v1 tests only the `;`-plus-`#` POSIX chaining pattern; other separators (`|`, `&&`, backticks, `$()`), Windows `cmd.exe`/PowerShell chaining, and quote-breaking prefixes for quoted injection contexts are explicitly not attempted. Issuable via `webguard-api permit issue --active-check active.cmdi.marker`. Evidence sanitization and fingerprint determinism are both PROVEN by dedicated unit tests (`test_command_injection_detector.py`, `test_finding_fingerprint_determinism.py::CommandInjectionFingerprintDeterminismTests`). See `docs/audit/active-detection-phase12-command-injection.md`. |
| CWE-611 | Improper Restriction of XML External Entity Reference (XXE) | `xxe_callback_detector.py` | **Registered, permit-gated, unit-tested. Not yet exercised end-to-end through the CLI/API/worker/executor pipeline, and not yet run against a live target.** Out-of-band (blind) confirmation only, reusing the identical `CallbackBroker` architecture `active.ssrf.callback` already uses: one crafted `application/xml` document per candidate, declaring a single external general entity whose SYSTEM identifier is a WebGuard-issued callback URL, replaces the endpoint's entire POST-form/JSON request body (candidates are deduped by endpoint+method rather than by parameter, since the crafted document discards whichever field originally discovered the endpoint). CONFIRMED requires a genuine inbound HTTP request at WebGuard's own receiver correlated to that exact token within the bounded wait window, the same confirmation logic SSRF already uses; response text is never inspected for classification, so a hardened parser's own safe-rejection error (which often reuses the same "DOCTYPE"/"external entity" vocabulary a vulnerable parser's failure could produce) can never be misread as evidence. v1 tests only this one payload shape (a single entity, referenced once, from the document root); in-band file-disclosure and parameter-entity-based exfiltration are explicitly excluded, not silently dropped (see the module's own docstring for why each was ruled out). Issuable via `webguard-api permit issue --active-check active.xxe.callback`. Evidence sanitization and fingerprint determinism are both PROVEN by dedicated unit tests (`test_xxe_callback_detector.py`, `test_finding_fingerprint_determinism.py::XxeFingerprintDeterminismTests`). See `docs/audit/active-detection-phase13-xxe-callback.md`. |
| CWE-601 | URL Redirection to Untrusted Site ('Open Redirect') | `open_redirect_detector.py` | **Registered, permit-gated, unit-tested. Not yet exercised end-to-end through the CLI/API/worker/executor pipeline, and not yet run against a live target.** Single diagnostic request per candidate (no baseline): submits an absolute `https://<fresh-random-marker>.invalid/` URL as the parameter's value and requires the response's status code to be one of the five browsers actually auto-follow (301/302/303/307/308, the WHATWG Fetch spec's own redirect-status set, deliberately excluding 300/305/306) with exactly one `Location` header that `urlsplit(...).hostname` resolves to that exact marker host. Structural hostname comparison, not substring matching, is the load-bearing property: it correctly resolves a classic userinfo-prefix trick (`https://target.com@<marker>.invalid/`) to the real destination host, and correctly does not flag a safe pattern where an app redirects to itself while merely echoing the value back inside its own query string. Two or more `Location` headers on one response are treated as an unresolved ambiguity (never a coin-flip pick), not evidence. Issuable via `webguard-api permit issue --active-check active.openredirect.location`. v1 has real, named gaps beyond the usual encoding/JS-redirect exclusions: it cannot observe a "store now, redirect later" pattern (the common post-login/post-SSO redirect, where the payload is captured on one request and only used on a separate, later one), does not check the legacy HTTP `Refresh` response header, and inherits this codebase's existing discovery-pipeline restrictions (crawl-mode's per-page path filter, and the shared `_STATE_CHANGING_KEYWORDS` safety classification that keeps POST-based logout/payment-callback redirect fields out of every active detector) in a way that costs this specific technique more than the others, since redirect-controlling parameters are classically discovered via a link to a *different* endpoint or present only in the entry URL's own query string. See the module's own docstring for the complete list. Evidence sanitization and fingerprint determinism are both PROVEN by dedicated unit tests (`test_open_redirect_detector.py`, `test_finding_fingerprint_determinism.py::OpenRedirectFingerprintDeterminismTests`). See `docs/audit/active-detection-phase14-open-redirect.md`. |
| CWE-90 | Improper Neutralization of Special Elements used in an LDAP Query ('LDAP Injection') | `ldap_injection_detector.py` | **Registered, permit-gated, unit-tested. Not yet exercised end-to-end through the CLI/API/worker/executor pipeline, and not yet run against a live target.** Structurally identical to `active.sqli.error`'s own baseline/diagnostic methodology: one diagnostic payload per candidate, a single unescaped closing parenthesis appended to the baseline value (`<baseline>)`, not a bare replacement, so a validation layer expecting a plausible-looking value is less likely to reject it before it ever reaches the vulnerable filter-construction code), classified by whether a specific, directory-client-library-or-server error signature newly appears in the diagnostic response (absent from baseline). The 13-entry signature list (PHP, Java/JNDI/UnboundID/Novell/Apache Directory, .NET, Python, Node.js) was fact-checked before implementation against current library source and real production incident reports, not assumed from memory; see the module's own docstring for exactly what each entry covers and its one bare, non-attributable signature (`"bad search filter"`). Real, disclosed safety consideration: on a target built with the Node.js `ldapjs` library, this exact payload can crash the target's process outright via a documented, uncaught synchronous exception, a materially larger blast radius than any other diagnostic payload this project ships. v1 tests only filter-syntax-error induction via this one payload; a separate DN-injection sub-technique (different metacharacters, corrupting a distinguished name rather than a search filter), boolean-based blind detection, and time-based detection are all explicitly not attempted. Unlike CWE-601/611/78/22, this CWE was not previously tracked in this registry's own "Planned" list at all; it is new scope added directly at the user's request, not a promotion from a pre-existing entry. Issuable via `webguard-api permit issue --active-check active.ldapi.error`. Evidence sanitization and fingerprint determinism are both PROVEN by dedicated unit tests (`test_ldap_injection_detector.py`, `test_finding_fingerprint_determinism.py::LdapInjectionFingerprintDeterminismTests`). See `docs/audit/active-detection-phase15-ldap-injection.md`. |
| CWE-306 | Missing Authentication for Critical Function | `missing_authentication_detector.py` | **Operator-reachable + authorization-controlled + end-to-end verified.** The precise, safely-testable child of the broader CWE-287 (Improper Authentication) the user actually asked for ("broken authentication"): proving a genuine authentication *bypass* would require completing an unauthorized login, destructive testing `ROADMAP.md` excludes by design; proving a resource is reachable with *no* authentication at all is a single anonymous GET, the same non-destructive shape every other active detector's probe already uses. CWE-287 itself stays in Planned below; this row does not close it. Unlike every other detector in this table, its candidates are never discovered by crawling or by any generic parameter fuzzer: an operator supplies an explicit `missing_authentication_endpoints` list (up to 10 entries) directly on the signed TrustScan permit itself, a new claim that required a schema bump (`1.3` → `1.4`, `MissingAuthenticationEndpoint`, replaced outright rather than dual-supported, matching this project's own established precedent from the `1.0`→`1.1` bump and its stated "not yet a publicly hosted production service" justification). For each endpoint, one authenticated baseline GET and one fully anonymous GET (no Authorization/Cookie header at all) are compared. Because this detector has only one real identity and no second identity's own baseline to serve as a negative control the way `active.authorization.idor` has, a bare content-fingerprint match alone is capped at PROBABLE: CONFIRMED requires *both* an exact fingerprint match *and* an operator-configured `owner_marker` (the identical opt-in mechanism `AuthorizationResource.owner_marker` already uses) present in the anonymous response, so a coincidentally identical public page (a CDN asset, an unpersonalized SPA shell) cannot alone produce a CONFIRMED finding. A marker match on a non-identical response (the common case when a page embeds a per-request CSRF token or timestamp) is PROBABLE, not dropped to NOT_VULNERABLE. Both probes use `allow_redirect_status=True`, and a 3xx status is classified from the status alone before any body is ever read, so a correctly-gated redirect-to-login is NOT_VULNERABLE, never misclassified as ERROR. One bad or off-origin endpoint among several is isolated to its own ERROR outcome and never discards another endpoint's already-computed result, a gap a pre-implementation adversarial review found in `active.authorization.idor`'s own executor wrapper and this detector deliberately does not repeat. Runs regardless of scan mode at the detector-module level, but the executor restricts it to single-page scans for a genuine contract reason, not a candidate-discovery one: `CrawlScanResult` has no scan-wide `findings` field at all (only per-page), and this detector's candidates were never page-specific to begin with, so there is no page to attribute a crawl-mode finding to even in principle (see the module's own docstring). Issuable via `webguard-api permit issue --active-check active.authentication.missing --authentication-context-id <id> --missing-auth-endpoint <url> [--missing-auth-marker <text>]` (repeatable, paired by position), gated by the same owner-only `PERMIT_ISSUE_ACTIVE` RBAC permission and the same fail-closed `active_checks` mechanism every other active detector uses; the permit contract itself additionally, bidirectionally requires the check ID, a non-empty endpoint list, and a set `authentication_context_id` to all be present together, self-contained in `TrustScanPermitClaims.__post_init__` with no live-repository lookup needed. Verified over a real, purpose-built local HTTPS fixture (self-signed cert, real sockets) with a genuinely vulnerable endpoint (identical marked content to any caller) and a properly-gated endpoint (401 without the correct bearer token); the properly-gated endpoint and a passive/inactive permit against the same vulnerable endpoint both correctly produce zero findings. A true end-to-end test (CLI → HTTP API → worker → executor → report) is verified the same way as CWE-79/CWE-89/CWE-639/CWE-918. Not yet run against a live/public target. Evidence sanitization and per-endpoint isolation are both PROVEN by dedicated unit tests (`test_missing_authentication_detector.py`); the permit-claim contract (the `MissingAuthenticationEndpoint` dataclass, the bidirectional binding rule, the schema round trip) is separately PROVEN by `test_trustscan_permit_contract.py`. See `docs/audit/active-detection-phase17-missing-authentication.md`. |

**10 CWEs implemented as active detectors, registered and unit-tested; the original 4, CWE-639 additionally against a live target, and now CWE-306 are also end-to-end verified through the full permit/executor pipeline. The 5 added in Slices 12-15 are not yet taken through that same end-to-end and live-target verification: see each row's own note.**

## Partial

Detected by a passive, heuristic-only check: real evidence when it fires, but absence of the signal is never proof of absence of the weakness. See the linked audit doc for exactly what the heuristic can and cannot see.

| CWE | Name | Detected by | Notes |
|---|---|---|---|
| CWE-352 | Cross-Site Request Forgery (CSRF) | `html_analyzer.py` | **Passive, name-based heuristic only, deliberately not an active detector.** True active confirmation would require actually completing a real state-changing action without a valid token, an unavoidably destructive test this project's own `ROADMAP.md` excludes by design; every other active detector's diagnostic payload in this codebase is deliberately non-destructive regardless of vulnerability status, a property CSRF confirmation structurally cannot share. Instead, every discovered POST form is checked for a hidden input matching one of 9 real, fact-checked anti-CSRF token naming conventions (Django, Rails, ASP.NET, Laravel/Symfony, Spring Security, WordPress, Drupal, Magento, ThinkPHP); a match also requires a non-empty, non-template-placeholder value, so a stale or broken template does not silently suppress the finding. No match produces a MEDIUM-severity, LOW-confidence finding, explicitly worded as a weak signal, not proof: the check cannot see SameSite-cookie-based protection (already checked separately under CWE-1275), Origin/Referer validation, a custom header attached by JavaScript, a `<meta>`-tag-plus-fetch pattern, or a deliberately renamed token field (WordPress's and Drupal's own security guidance recommends exactly that rename). Apache Struts 2's own default field name, the bare word `token`, is deliberately excluded from the list: both lenses of this check's own pre-implementation review confirmed it collides with unrelated one-time-link fields (`reset_token`, `api_token`) in the dangerous direction (falsely suppressing a real finding). Evidence sanitization is PROVEN by a dedicated unit test. See `docs/audit/active-detection-phase16-csrf-token-heuristic.md`. |

**1 CWE partial.**

## Planned

The remaining active-detection target set (see `ROADMAP.md`'s "Coverage growth" section), gated behind a valid TrustScan permit and the `SecurityCheck` interface described there. Not yet implemented.

| CWE | Name | Category |
|---|---|---|
| CWE-502 | Deserialization of Untrusted Data | Injection |
| CWE-287 | Improper Authentication | Authentication |
| CWE-862 | Missing Authorization | Authorization |

**3 CWEs planned.** CWE-601 moved to Implemented (active) in an earlier slice; CWE-352 moved to Partial in Slice 16. CWE-287 itself stays here deliberately: Slice 17 implements its precise child CWE-306 (Missing Authentication for Critical Function), not CWE-287 itself, the identical precision discipline this registry already applies to CWE-639-not-862 for IDOR. A genuine CWE-287 test (proving a target *accepts* credentials it should reject) would require actually attempting a login, which shades into the password-attack territory `ROADMAP.md` explicitly excludes; CWE-287 remains Planned until a safe, non-destructive shape for that specific claim is identified, if one exists.

## Not yet classified

Every other CWE, including the full breadth of the reference catalog consulted while scoping this list, is treated as **NOT APPLICABLE to external black-box assessment** unless a specific, evidence-backed detection approach is proposed and reviewed. This registry will only grow by adding rows with real detection logic and tests attached (see `docs/ROADMAP.md`, "Detection architecture and CWE coverage"; no CWE is added to this file on the basis of intent alone).

## Coverage summary

```
18 Implemented (passive, in orchestration)
10 Implemented (active, permit-gated, integrated into orchestration)
 1 Partial
 3 Planned
```

## Non-CWE identifiers

As of Slice 8, findings may also carry an `OWASP-API` identifier alongside their primary CWE: the OWASP API Security Top 10 (2023) classifies authorization-bypass findings (e.g. `API1:2023`, Broken Object Level Authorization) more specifically for API-shaped resources than any single CWE does. As of Slice 10, findings may also carry a plain `OWASP` identifier (e.g. `A10:2021`) referencing the OWASP Top 10 (2021) itself, used for SSRF since that list's own A10 category is literally named "Server-Side Request Forgery." Both are additive: every such finding still carries a CWE as its primary identifier, and neither `OWASP-API` nor `OWASP` is ever a substitute for one.

## Slice 11 stabilization audit trail

This registry was cross-checked against the actual code (every `ExternalIdentifier("CWE", ...)` call site in every analyzer/detector module) and the actual test suite, not re-derived from memory or prior documentation. Result: **no drift found**. Every CWE row above cites a module that exists and actually carries that identifier; every passive/active module that carries a CWE identifier is cited somewhere above. Every IMPLEMENTED mapping traces cleanly through detector/check → regression test → evidence model → finding serialization (see `docs/scanner/SCANNER_V1_CAPABILITIES.md` for the per-capability test citations, and `docs/audit/scanner-v1-false-positive-review.md` for the classification-discipline audit).

One completeness gap, not a correctness error, was found: `html_analyzer.py`'s `web.html.meta_refresh.*` check family produces a real finding but carries no `identifiers=`/CWE tag at all. The check exists and fires, it simply isn't represented in either CWE table above. Recorded here rather than silently left as an invisible gap.

## Slice 12: path traversal and command injection added post-freeze

`docs/scanner/SCANNER_V1_CAPABILITIES.md` calls Slice 11 "the Scanner v1 feature freeze." This slice adds two new active detectors (`active.pathtraversal.disclosure`/CWE-22, `active.cmdi.marker`/CWE-78) after that freeze, at the user's explicit request, following the exact same registry/permit-authorization architecture the frozen four already use (`ACTIVE_DETECTOR_REGISTRY`, `KNOWN_TRUSTSCAN_ACTIVE_CHECKS`, the generic per-page detector loop). Stated plainly rather than left implicit: this is new scope added on top of the v1 freeze, not a v1 correction. Both are registered, contract-synced (`test_active_detector_registry.py` passes unmodified), and unit-tested (classification logic, evidence sanitization, fingerprint determinism, and the full same-origin/budget/cancellation/hook safety-boundary suite already required of every other active detector), but have not yet been taken through the CLI → HTTP API → worker → executor end-to-end path the original four were, and have not been run against any live target. See `docs/audit/active-detection-phase11-path-traversal.md` and `docs/audit/active-detection-phase12-command-injection.md`.

Detector-registry integrity was also verified this slice: the scanner-side registry (`ACTIVE_DETECTOR_REGISTRY` ∪ `COMPARISON_ACTIVE_CHECK_IDS` ∪ `CALLBACK_ACTIVE_CHECK_IDS`) and the contracts-side `KNOWN_TRUSTSCAN_ACTIVE_CHECKS` tuple are asserted exactly equal by `tests/unit/test_active_detector_registry.py`, and the CLI/API's `--active-check`/`active_checks` validation traces through exactly one call site (`scan_permits.py`'s `_active_checks()`) with no separate or stale check-ID list anywhere in `cli.py`/`service.py`. No undocumented detector is executable; no removed detector remains permit-authorizable (there have been none removed to date).

## Slice 13: XXE (out-of-band) added post-freeze

Adds one more active detector after the same Slice 11 freeze, at the user's continued request: `active.xxe.callback` for CWE-611. Unlike Slice 12's two additions, this one does not fit `ACTIVE_DETECTOR_REGISTRY`'s generic per-page calling convention: confirming XXE out-of-band needs a `CallbackBroker` registration and a bounded wait for an outbound callback, the identical shape `active.ssrf.callback` already required. It therefore joins `CALLBACK_ACTIVE_CHECK_IDS` (not `ACTIVE_DETECTOR_REGISTRY`) and gets its own executor function, `_apply_xxe_callback_detection`, a close mirror of `_apply_ssrf_callback_detection`.

The design deliberately covers only one variant: out-of-band (blind) XXE, confirmed by a genuine inbound HTTP request at WebGuard's own callback receiver. In-band file-disclosure and in-band error-signature detection were both considered and explicitly ruled out, not silently dropped: response text is never inspected for classification at all, so there is no signature vocabulary for a hardened parser's own safe rejection (which often reads a lot like a vulnerable parser's failure) to be misread as evidence. `request_template.py` gained one new named constant (`ContentType.XML`) but no new validation; the probe's `MutatedRequest` is built directly rather than through `mutate()`, since XXE replaces the entire request body and content type rather than substituting one value, and nothing on the `issue_templated_request`/`fetch_once` path requires a `MutatedRequest` to have come from `mutate()` in the first place.

Registered, contract-synced (`test_active_detector_registry.py` passes unmodified aside from the new set membership), and unit-tested (payload shape, classification logic including a hardened-parser false-positive control, evidence sanitization, fingerprint determinism, and the same-origin/budget/cancellation/hook safety-boundary suite every other active detector already requires), but not yet taken through the CLI → HTTP API → worker → executor end-to-end path, and not yet run against any live target. See `docs/audit/active-detection-phase13-xxe-callback.md`.

## Slice 14: open redirect added post-freeze

A fourth post-freeze active detector, `active.openredirect.location` for CWE-601, added the same way, at the user's continued request. Unlike XXE, it fits `ACTIVE_DETECTOR_REGISTRY`'s generic per-page calling convention exactly (one synchronous request per candidate, no `CallbackBroker`, no wait), the same shape SQLi/XSS/path traversal/command injection already use.

Before writing any code, a drafted classification design was adversarially reviewed by three independent lenses hunting specifically for false positives, in-scope false negatives, and code-level bugs. None found a way to produce a false CONFIRMED against a genuinely safe target, but the review did surface, and this slice fixed before implementation, three real issues: duplicate `Location` headers being resolved by picking whichever came first rather than treating the ambiguity as inconclusive, a trailing-dot FQDN-canonicalised host not matching the expected host, and 300/305/306 being treated identically to the five statuses browsers actually auto-follow. The review also surfaced several real, disclosed-not-fixed gaps in this technique's reach, inherited from this codebase's existing discovery pipeline rather than introduced by this detector: crawl-mode's per-page path restriction and the shared state-changing-keyword safety classification both cost open redirect specifically more than they cost the other techniques, since redirect-controlling parameters are classically discovered via a link to a different endpoint, or present only in the entry URL's own query string. All of this is stated directly in the detector module's own docstring, not left implicit.

This slice also added one new capability to the shared transport layer used by every active detector: `allow_redirect_status` (an existing `fetch_once` parameter, previously only used by the login workflow) is now threaded through `issue_probe` and `issue_templated_request` as a defaulted-False keyword-only argument, so a 3xx response can be read intact instead of becoming a `redirect_blocked` error. Every existing caller leaves it False and is completely unaffected; `active.openredirect.location` is the first candidate-based detector to pass True.

Registered, contract-synced, and unit-tested (payload shape, classification logic including the userinfo-trick, safe-query-string-echo, duplicate-header, and malformed-IPv6 false-positive controls, evidence sanitization, fingerprint determinism, and the same-origin/budget/cancellation/hook safety-boundary suite every other active detector already requires), but not yet taken through the CLI → HTTP API → worker → executor end-to-end path, and not yet run against any live target. See `docs/audit/active-detection-phase14-open-redirect.md`.

## Slice 15: LDAP injection added post-freeze

A fifth post-freeze active detector, `active.ldapi.error` for CWE-90, added the same way, at the user's continued request. Unlike CWE-90's four predecessors in this slice sequence, it was never previously tracked in this registry's own "Planned" list at all: it is new scope added directly, not a promotion from a pre-existing entry. It fits `ACTIVE_DETECTOR_REGISTRY`'s generic calling convention exactly, structurally near-identical to `active.sqli.error`'s own baseline/diagnostic/signature-matching architecture, reused unchanged apart from the payload and signature list.

Before writing any code, a drafted signature list and payload were fact-checked and stress-tested by three independent lenses (a signature fact-checker, a false-positive hunter, and a payload-reliability/false-negative hunter), each verifying claims against real client-library source and real production incident reports rather than reasoning from memory. All 9 originally-drafted signatures (PHP, Java/JNDI/UnboundID/Novell, .NET) checked out as real and accurate, including resolving the drafter's own stated uncertainty about whether PHP 8 had moved this warning path to exceptions (it has not, confirmed against current php-src). The review added 4 more signatures the draft was missing entirely (Python's two major LDAP clients, the Apache Directory API/ApacheDS stack, and Node.js's ldapjs), and caught one real design ambiguity before it became a bug: the draft's own prose said the diagnostic payload should be appended to the baseline value, but the sibling detector it was modeled on (`sqli_error_detector.py`) actually uses a bare replacement constant, not an append pattern, a distinction that could have been reflexively copy-pasted wrong. The shipped detector explicitly appends, resolving the ambiguity in code as well as in prose.

The review also surfaced a real, disclosed-not-mitigated safety consideration materially different in kind from anything this project has shipped before: on a target built with the Node.js `ldapjs` library, this exact payload can crash the target's entire process via a documented, uncaught synchronous exception in that library's own filter parser, rather than merely produce an error response. Every other active detector's diagnostic payload, including this one's own siblings, can at most produce an unusual response; none of them can crash the target process outright. This is stated plainly in the detector's own docstring rather than softened, matching this project's own rule that a safety claim names the actual mechanism rather than a wrapper of hedged language.

Registered, contract-synced (`test_active_detector_registry.py` passes unmodified aside from the new set membership), and unit-tested (classification logic, append-not-replace payload construction, every documented signature confirmed matched, evidence sanitization, fingerprint determinism, and the same-origin/budget/cancellation/hook safety-boundary suite every other active detector already requires), but not yet taken through the CLI → HTTP API → worker → executor end-to-end path, and not yet run against any live target. See `docs/audit/active-detection-phase15-ldap-injection.md`.

## Slice 16: CSRF added as a passive check, not an active detector

A sixth post-freeze addition, at the user's continued request, but a different shape from every one before it: `web.html.csrf_token` for CWE-352 is a passive check added to the existing `html_analyzer.py`, not a new active detector.

The reason is structural, not a style preference. Every active detector added in Slices 12-15 (path traversal, command injection, XXE, open redirect, LDAP injection) sends a diagnostic payload deliberately engineered to be non-destructive regardless of whether the target is vulnerable: a syntax-breaking character, a callback to WebGuard's own infrastructure, a redirect the transport never follows. Confirming CSRF the same active way would require actually submitting a well-formed state-changing request without a valid token and observing whether the action completes; if the target is genuinely vulnerable, that observation IS the action completing for real. There is no payload shape that makes this non-destructive, which is exactly the "active exploitation... destructive testing... not part of the current initial release scope" line `ROADMAP.md` already states. Read directly, `attack_surface.py`'s own `_classify_safety` keyword list also does not fully block this (only endpoints matching specific keywords like "payment" or "delete" are excluded; a great many real state-changing forms aren't caught by that list and remain reachable), which confirms the exclusion here is about what the *technique* requires, not merely about which endpoints discovery happens to reach.

Instead, this slice extends `html_analyzer.py`'s existing form-parsing (`_BoundedHtmlParser`/`_FormRecord`, already used for the password-transport and cross-origin-form-action checks) to track hidden-input fields and flag a POST form with no field matching a recognized anti-CSRF token naming convention. Before writing any code, the token-naming-convention list and the matching approach were surveyed and adversarially stress-tested by two independent lenses: one researching real, current default field names across mainstream frameworks (using live web search, not memory), the other hunting specifically for false positives given the actual parser code, not just the design's prose. Both converged on the same core verdict: the exact-match-not-substring choice for ambiguous short names (`_token`, and the three names added from this review: `form_token`, `form_key`, `_wpnonce`, plus `__token__`) correctly avoids colliding with unrelated one-time-link fields like `reset_token`/`api_token`, and Apache Struts 2's own default field name, the bare word `token`, is deliberately left out of the list entirely because both lenses independently confirmed it collides with exactly that class of field. The review also caught a real gap in the original draft, the matcher only inspected field names and never values, meaning a stale cache or broken template could ship the right field name with an empty or unrendered placeholder value and wrongly suppress the finding; the shipped check requires a non-empty, non-placeholder value before treating a name match as evidence of protection.

Confidence is LOW (this project's weakest tier), severity MEDIUM, and the finding's own description states plainly that absence of a recognized name is not proof of absence of protection: the check cannot see SameSite-cookie-based defenses (already covered separately by `cookie_analyzer.py` under CWE-1275), Origin/Referer validation, a custom header attached by JavaScript, or a `<meta>`-tag-plus-fetch double-submit pattern, and two of the highest-market-share platforms on the web, WordPress and Drupal, both publish their own security guidance recommending developers rename the token field away from the very defaults this check recognizes, specifically so neither an attacker nor a scanner like this one can rely on them. None of this is hedging for its own sake: it is the honest ceiling of what a static, name-based heuristic can ever claim, stated directly rather than smoothed over.

Unit-tested (12 new tests covering the naming-convention matches, the value-placeholder guard, the reset_token/api_token collision-avoidance the exact-match choice exists for, evidence sanitization, and per-form independence), with the pre-existing `html_analyzer.py` suite updated where a test's own scope predated this check (one test now includes a token field so it stays scoped to the password checks it was written to prove, not incidentally exercising the new one). See `docs/audit/active-detection-phase16-csrf-token-heuristic.md`.

## Slice 17: missing authentication (CWE-306) added, with a permit schema change

A seventh post-freeze addition, at the user's continued request for "broken authentication." Unlike every prior post-freeze slice, this one could not stay self-contained inside the scanner package: the user was asked, via a direct question, whether to build the full version (a new signed-permit claim plus CLI/service/executor wiring) or a narrower alternative, and chose the full build.

CWE-287 (Improper Authentication) itself has no safe active-detector shape: proving a target incorrectly *accepts* bad credentials requires actually attempting a login, which is the password-attack territory `ROADMAP.md` already excludes. The concrete, honestly-testable instance is CWE-306 (Missing Authentication for Critical Function): an operator asserts that a specific URL should require a specific authenticated identity, and the detector checks whether it is reachable with no authentication at all. This is the same precision discipline this registry already applies to CWE-639-not-862 for IDOR: the most specific CWE that matches exactly what was proven, not the broader CWE the user's own request happened to name.

Because the detector's candidates are operator-asserted, permit-scoped endpoints rather than crawled or fuzzed values, they needed a new home. A research pass confirmed no existing ad-hoc channel exists for a URL list, and that a direct, bounded claim on the permit itself (rather than a separate plan/repository, the heavier pattern `active.authorization.idor` uses for its own two-identity resource pairs) is the right fit: this claim holds no secrets, needs no independent revocation, and this project's own `authorization_comparison.py` docstring already states the design philosophy this follows ("the smallest clean mechanism"). `CURRENT_TRUSTSCAN_PERMIT_SCHEMA_VERSION` moved `1.3` → `1.4`, replaced outright rather than dual-supported, the same precedent the `1.0`→`1.1` bump set and justified the same way (no deployed old-schema permit to keep compatible). The new field, `missing_authentication_endpoints` (a tuple of `MissingAuthenticationEndpoint`, mirroring `permitted_modes`'s "enum tuple, not bare strings" shape), is enforced bidirectionally and self-containedly inside `TrustScanPermitClaims.__post_init__`: the check ID, a non-empty endpoint list, and a set `authentication_context_id` must all be present together, with no live-repository lookup needed, a stricter and more symmetric rule than IDOR's own analogous cross-check (which only verifies the forward direction, and only once, at issuance time).

Before writing the detector itself, a two-agent adversarial verification pass reviewed the drafted design against real code (not just its own prose) and found one blocking issue and several should-fix issues, all resolved before implementation:

- **Blocking**: unlike `active.authorization.idor`, this detector has no second identity's own baseline to serve as a negative control, so a bare content-fingerprint match promoted straight to CONFIRMED would false-positive on a legitimately public/identical page (a CDN asset, an unpersonalized SPA shell). Fixed by reusing IDOR's own opt-in `owner_marker` mechanism: CONFIRMED now requires both an exact fingerprint match *and* a configured marker present in the anonymous response; either alone is PROBABLE.
- Dynamic per-request content (a CSRF token, a timestamp) would make a genuinely leaking page's authenticated and anonymous bodies differ byte-for-byte, producing a false NOT_VULNERABLE with no fallback. The same marker mechanism fixes this: a marker match on a non-identical response is PROBABLE, not dropped.
- The drafted design assumed a 3xx redirect-to-login would classify cleanly, but `fetch_once` raises `redirect_blocked` for any 3xx unless the caller explicitly passes `allow_redirect_status=True`. Fixed by passing it for both probes and classifying any 3xx from its status alone, before ever reading a body.
- `active.authorization.idor`'s own executor wrapper catches `ActiveDetectionError` around its *entire* per-pair loop, so one malformed resource silently discards every other pair's results, an existing, unfixed, untested gap. This detector deliberately does not repeat it: each endpoint's same-origin check and fetch is wrapped in its own local try/except, isolating a bad entry to that one endpoint's ERROR outcome.
- `active.authorization.idor`/`active.ssrf.callback`/`active.xxe.callback` all skip crawl-mode scans in the executor, each citing page-attribution as the reason. Investigation for this slice found the real, underlying reason is structural, not attribution: `CrawlScanResult` has no scan-wide `findings` field at all (only `ScanResult` does; a crawl report's findings live only inside each individual page in its own `pages` tuple). This detector's candidates were never page-specific to begin with, so there is no page, plausible or otherwise, to attribute a crawl-mode finding to: a stronger reason to restrict to single-page scans than any of its three predecessors had, not a weaker one. Stated explicitly in the detector module's own docstring rather than left as an unexplained copy of the existing pattern.
- Reusing `_apply_active_detection`'s single-identity resolution pattern (resolve once, up front, outside any per-candidate loop; let `AuthenticationContextError` propagate as a scan-level `TrustScanRuntimeSafetyError` rather than being caught per-endpoint) was confirmed as the correct, already-proven-safe precedent, not a new risk to design around.

A new, distinctly-named detector-dispatch category, `FIXED_ENDPOINT_ACTIVE_CHECK_IDS`, was added to `active_detector_registry.py` rather than overloading `COMPARISON_ACTIVE_CHECK_IDS`: that category's own docstring defines "comparison" specifically as IDOR's two-identity, resource-*pair* correlation, a shape this detector (one real identity vs. no identity, flat endpoints, never pairs) does not share. `KNOWN_ACTIVE_DETECTOR_IDS` is the union of all four categories now, and `test_active_detector_registry.py`'s sync assertions were extended to check all pairwise disjointness, not just the original three.

Registered, contract-synced, and unit-tested at every layer: the permit-claim contract (`test_trustscan_permit_contract.py`, 17 new tests: the `MissingAuthenticationEndpoint` dataclass, its `to_dict`/`from_dict` round trip, the bidirectional binding rule's every path, the maximum-10 bound, JSON submission round-tripping, fingerprint sensitivity to the new field); the detector itself (`test_missing_authentication_detector.py`, 12 new tests: CONFIRMED requiring both signals, PROBABLE for either signal alone, the redirect/denial/generic-200 NOT_VULNERABLE paths, INCONCLUSIVE on baseline failure, per-endpoint ERROR isolation, anonymous-probe identity isolation, evidence sanitization, budget/cancellation); and, unlike every detector added since the v1 freeze, a true end-to-end test (`tests/integration/test_missing_authentication_e2e_lab.py`, real HTTPS fixture, real permit → job → worker → executor → report pipeline) proving a genuinely vulnerable endpoint reaches CONFIRMED and a properly-gated endpoint and an inactive permit both produce zero findings. Not yet run against a live/public target. See `docs/audit/active-detection-phase17-missing-authentication.md`.
